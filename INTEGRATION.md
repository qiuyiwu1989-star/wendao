# 把问道集成进深脑 · 交接文档

> 给深脑侧实现「问道频道」的人。**这份文档的价值 = 让你不用把我踩过的坑再踩一遍。**
> 问道独立版的源码在 https://github.com/qiuyiwu1989-star/wendao
> 线上参考实现：https://wendao.zaowuyun.com/chat

---

## 一、直接拷过去就能用的（不用重写）

| 资产 | 文件 | 说明 |
|---|---|---|
| **大脑** | `SKILL.md` + `methods/*.md` | 纯文本提示词，问道的全部思考方式都在这。**改它 = 改问道怎么与人对话** |
| **提示词组装** | `lib/persona.ts` | 拼装逻辑 + 精简/完整两版 |
| **录音 + 静音检测** | `lib/recorder.ts` | 自包含，无外部依赖 |
| **句级流式播放** | `lib/speechQueue.ts` | 自包含，Web Audio 无缝拼接 |
| **三个 API** | `app/api/{asr,tts,chat}/route.ts` | 只是 MiMo 代理，逻辑很薄 |

**不要拷的**：`lib/{useAuth,supabase,conversations,db,deepbrain}.ts`、`app/api/{conversations,feed}`
——这些是独立版为了自己有账号/历史/跨应用取数才建的，深脑里全都有原生的、更好的。

---

## 二、必须知道的坑（血泪）

### 坑 1：浏览器自带的语音识别，国内用不了
Chrome 的 Web Speech API（`webkitSpeechRecognition`）**走谷歌服务器，国内被墙**。
我最初图省事用了它，结果通话模式永远卡在"在听你说……"，查了很久。

**正解**：浏览器录音 → 自己做静音检测 → WAV → 传服务端 → **MiMo ASR**。
> 通用教训：国内产品凡"浏览器自带的云能力"（语音识别、翻译等），先怀疑是不是走谷歌。

### 坑 2：MiMo 的 ASR / TTS 接口不在常规位置
`/v1/audio/speech`、`/v1/audio/transcriptions` 都是 **404**。两者都走 `/v1/chat/completions`：

**TTS**（文字→语音）：待读文本放 **assistant** 角色消息
```jsonc
{ "model":"mimo-v2.5-tts", "stream":true,
  "messages":[{"role":"assistant","content":"要读的文字"}],
  "audio":{"format":"pcm16","voice":"苏打"} }
// 流式：音频在 chunk.choices[0].delta.audio.data（base64 PCM16 24kHz 单声道）
// 非流式：format 用 wav，音频在 choices[0].message.audio.data
// 预置音色：苏打/白桦(男) 冰糖/茉莉(女) Mia/Chloe/Milo/Dean(英)
```

**ASR**（语音→文字）：音频放 **user** 消息的 `input_audio`
```jsonc
{ "model":"mimo-v2.5-asr",
  "messages":[{"role":"user","content":[
    {"type":"input_audio","input_audio":{"data":"<base64>","format":"wav"}}]}] }
// 转写在 choices[0].message.content
```

**对话**：`/anthropic/v1/messages`，Anthropic 兼容，可直接用 `@anthropic-ai/sdk` 设 baseURL。

### 坑 3：延迟的真凶是提示词，不是模型
完整提示词 ~8.8 万字，MiMo 冷缓存下要重算约 4 万 token，**首句能到 10-15 秒**。
提速三件套（缺一不可）：

1. **精简提示词**：语音模式只带 `SKILL.md`（~3.8 万字），砍掉 `methods/` 的元件细节。短对话用不上细节，引擎段已够。
2. **关掉思考**：`thinking: {type:"disabled"}`。MiMo 默认先思考再开口，对教练式短回答增益很小、延迟代价大。
3. **句级流式 TTS**：模型吐出**整句**（到句号/问号）就立刻送去合成朗读，不等整段。合成与播放重叠。

**模型选型**：用 `mimo-v2.5`，不要 `pro`。实测 pro 解码 ~7 tok/s、首句 2.7s；v2.5 26 tok/s、首句 1.4s，教练式短回答**质量不掉**。

**延迟基线**（服务器侧量，国内真实体感）：首字 **0.6–1.0s**，偶发 3s+ 是 MiMo 网关波动。
> 量延迟必须在服务器上量。本地 curl 打公网含跨境往返（+3s），量出来的是假象。

### 坑 4：通话模式只在"听"阶段开麦
思考和说话时**必须关麦**，否则会把问道自己的声音录进去（回声/自问自答）。
代价：没有"抢话打断"。补偿：点中间的圆圈可打断，立刻回到听。

### 坑 5：grounding 的超时必须是"硬竞速"
深脑检索单次 **3-4 秒**。只用 `AbortController` **掐不断**（请求已发出仍在等），
实测把语音首字拖到 **14 秒**。

正解：`Promise.race([检索, 定时器])` —— 到点立刻放行继续对话，**不等**在途请求；
后台让它跑完并写进缓存，**下一轮同话题直接命中**（0 延迟拿到 grounding）。

> 在深脑里这个坑会轻很多——不用走 MCP 那一跳，直接内部查库，应该能压到 1s 内。

### 坑 6（最重要）：grounding 必须按用户隔离
**我在独立版犯过这个错，造成了真实的数据泄露**：服务端只有一把深脑 key（代表 key 主人自己的大脑），
而 `/api/chat` 不校验登录 —— 结果**任何匿名访客提问都会读到 key 主人的私有判断**，
而且**陌生人的对话会被写进 key 主人的记忆库**。

在深脑里做就没这个问题（原生的每用户隔离），但仍要守住：
- 检索**只能查当前登录用户自己**的数据，作用域必须在服务端强制，不能信前端传的 id
- 投喂**只能写进当前用户自己**的记忆库
- 权限判断**默认关闭**（fail-closed），配置缺失时不要 fail-open

---

## 三、深脑里应该做得更好的地方

| 独立版的做法 | 深脑里应该怎么做 |
|---|---|
| 走 MCP `search_brain`（3-4s） | **直接内部查库**，省掉 MCP 一跳，快得多 |
| 单 key + 邮箱白名单（权宜之计） | **原生每用户隔离**，白名单可以整个删掉 |
| 投喂走 `ingest_transcript` | 直接写当前用户的记忆库 |
| 自己一套 Supabase 登录 | 用深脑现成的 |
| `wendao_conversations` 表存历史 | 并进深脑的会话存储 |

---

## 四、提示词里的几条硬约束（别改坏）

问道的"人格"全靠这些，集成时容易被无意破坏：

1. **安全边界是第 0 优先级**：用户表达严重痛苦时，**停掉所有教练动作**，接住 + 给全国心理援助热线 12356（紧急 120/110）。这条凌驾于"短""不给答案"之上。
2. **语音场景禁 Markdown**：回答会被朗读，不能出现表格/编号/星号/emoji。
3. **一回合只出一招**，说完把球踢回给用户。深度是一来一回磨出来的。
4. **方法隐性使用**，永远不报术语名（不说"从第一性原理看"）。
5. **grounding 的用法**：读到用户既有判断后，要**自然地用**，不要复述、不要说"根据你的记录/深脑显示"。像一个认识他很久的人。

---

## 五、验收清单

- [ ] 文字对话正常、逐字流式
- [ ] 语音朗读（男声流式，整句即读）
- [ ] 麦克风输入（需 HTTPS）、说完自动断句
- [ ] 通话模式：说完自动接话 → 说完自动接着听 → 可打断
- [ ] **grounding 只读到当前用户自己的判断**（换个账号验证读不到别人的）
- [ ] **投喂只写进当前用户自己的记忆库**
- [ ] 危机输入触发安全兜底（给 12356，不再教练式反问）
- [ ] 服务器侧量首字延迟 < 1.5s
